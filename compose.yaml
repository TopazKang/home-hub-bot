services:
  home-hub-bot:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: home-hub-bot

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "8211:8080"